machine:
  node:
    version: 5

dependencies:
  pre:
    - rm -rf node_modules

test:
  override:
    - npm run lint
    - npm run test:server
    - npm run build
    - npm start:
        background: true
    - npm run test:e2e
